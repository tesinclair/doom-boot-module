#include "mesh.h"

int
draw_mesh_box(
        void *double_buf,
        size_t x,
        size_t y,
        size_t z,
        size_t height,
        size_t width,
        size_t depth,
        size_t color
        ){
    return -1;
}

int
draw_mesh_bubble(
        void *double_buf,
        size_t x,
        size_t y,
        size_t z,
        size_t radius_x,
        size_t radius_y,
        size_t radius_z,
        size_t color
        ){
    return -1;
}

